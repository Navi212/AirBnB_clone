#!/usr/bin/python3
"""The `state` module defines a class `State`
that inherits from `BaseModel`."""


from models.base_model import BaseModel


class State(BaseModel):
    """Defines a class `State` that inherits from `BaseModel`."""
    name = ""
